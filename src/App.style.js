import styled from 'styled-components';

export const Title = styled.h1`
  color: #CC9B6D;
  font-family: Helvetica, arial, sans-serif;
  font-size: 18px;
  font-weight: bold;
`

export const ComplementaryGraphsWrapper = styled.div`
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  margin-top: 80px;
  gap: 40px;
`